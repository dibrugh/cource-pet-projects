{
  "@parcel/transformer-css": {
    "cssModules": true
  },
  "name": "marvel-comics",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel public/index.html --open",
    "build": "parcel build public/index.html",
    "deploy": "rm -rf dist/ && parcel build public/index.html --no-optimize --public-url ./"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "gh-pages": "^5.0.0",
    "parcel": "^2.9.3",
    "postcss-modules": "^6.0.0"
  },
  "dependencies": {
    "axios": "^1.4.0"
  }
}
